def power(x, n):
    """Compute the value x n for integer n."""
    if n == 0:
        return 1
    else:
        print("without calc:", x, n)
        print('input calc:', x, n-1)
        print("...................")
        return x * power(x, n-1)


print(power(2, 5), '\nwow it works')







